# macOS
.DS_Store

# Dependencies
node_modules

# TypeScript incremental build cache
*.tsbuildinfo

# Build output
public/
.quartz-cache/

# Site content is synced in from the work-projects vault by the deploy
# workflow at build time - never committed here (see README.md)
content/
